# Context Policy

Apply progressive context disclosure:

`AGENTS.md → CONTEXT.md → relevant project → relevant stage → references → specific files`.

Retrieve only what is necessary for the task. Prefer current operational context over historical material. If a task depends on an unresolved conflict, retrieve both relevant sources and make the conflict explicit.

Do not use conversation history as the sole source of durable workspace facts when a canonical repository source should exist.
