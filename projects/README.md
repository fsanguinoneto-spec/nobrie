# Projects

Each distinct Nobrie workstream gets an isolated project directory. Do not create a project until there is a real workstream to manage.

A project normally contains `CONTEXT.md`, `STATUS.md`, `TASKS.md`, `DECISIONS.md`, and `README.md`, with additional directories added only when functionally required.
