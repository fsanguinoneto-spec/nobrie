# /decision-log

Record a meaningful decision in the canonical project `DECISIONS.md`.

Each decision should include:
- ID
- date
- status
- context
- decision
- reason
- alternatives considered, when relevant
- consequences
- affected files

When a decision changes, preserve the prior record and mark it `SUPERSEDED` rather than silently rewriting history.
